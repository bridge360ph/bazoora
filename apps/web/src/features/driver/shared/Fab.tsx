type FabProps = {
  onNavigate?: (key: string) => void;
};

// FAB placeholder.
// The mobile FAB is currently disabled because its destinations
// are available through the header and bottom navigation.
export function Fab(props: FabProps) {
  void props;
  return null;
}